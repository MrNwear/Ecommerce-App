import { ScaledSheet } from "react-native-size-matters";

export const styles = ScaledSheet.create({
    container: {
        margin: '10@s'
    }
    ,
    headerText: {
        fontWeight: '700',
    }
})